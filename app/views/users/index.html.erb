<% @users.each do |user| %>

  <pre><%= user.inspect %></pre>

    <%= link_to 'Edit', edit_user_path(user) %>
    <%= link_to 'Delete',
        user_path(user),
        method: :delete, data: { confirm: 'Really?' } %>

<% end %>